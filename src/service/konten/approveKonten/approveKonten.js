import "../../../utils/api/api";
import { baseURL } from "../../../utils/api/constan";

// export const postPendapatan = async (data) => {
//   // const response = (await api.post) < any > (`${basePath}/pendapatan`, data);
//   // const response = (await api.get)(`{bas}`, data);
//   return response.data.return;
// };
